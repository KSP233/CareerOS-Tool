# CareerOS 本地版本交接说明

更新时间：2026-08-27（America/Toronto）

## 1. 新任务目标

继续维护和完善 CareerOS：一个 Windows 本地运行、手动控制的求职辅助程序。

核心边界：

- 搜索、整理、筛选和分析职位。
- 可选 AI 分析、中文翻译、简历草稿和 Cover Letter 草稿。
- 用户亲自审核并打开职位网页。
- 不实现自动填写、自动登录或自动投递。
- 不把简历、数据库、个人资料或 API key 放进公开测试包。

## 2. 实际源码和启动位置

- 本地源码：`C:\Users\ksp23\Desktop\ApplyPilot`
- 主程序：`C:\Users\ksp23\Desktop\ApplyPilot\main.py`
- 双击启动：`C:\Users\ksp23\Desktop\ApplyPilot\START.cmd`
- 调试启动：`C:\Users\ksp23\Desktop\ApplyPilot\START-DEBUG.cmd`
- 项目自带 Python：`C:\Users\ksp23\Desktop\ApplyPilot\runtime\python.exe`
- 当前本机数据位置：`D:\ApplyPilot`

`START.cmd` 使用项目内的 `runtime\pythonw.exe` 启动，因此不依赖用户的全局 Python。

## 3. 当前技术结构

- GUI：PySide6 6.9.2
- 职位搜索：python-jobspy 1.1.82
- 数据库：SQLite，默认文件名仍为 `applypilot.db`
- PDF：ReportLab + PySide6 QtPdf
- 文档读取：pypdf、python-docx、openpyxl
- 密钥保护：cryptography/Fernet
- AI：可选本地 Ollama 或 OpenAI-compatible API

主要文件：

- `app/gui.py`：所有页面、导航、表格、后台任务线程和 macOS/Windows 风格界面。
- `app/services.py`：职位搜索、初步评分、AI 分析、翻译、简历和 Cover Letter 生成。
- `app/ai_manager.py`：本地/API 模型选择和调用。
- `app/database.py`：SQLite schema、职位、简历版本、Cover Letter、申请记录和 AI 请求记录。
- `app/pdf_export.py`：简历和 Cover Letter PDF 排版。
- `app/secrets.py`：API key 本地加密。
- `config.py`：数据路径、默认搜索词、地点、距离、AI 设置和目录迁移。

依赖版本记录在 `requirements.txt`。

## 4. 当前产品功能

### Dashboard

- 统计 Jobs Found、Good Matches、Ready、Applied、Interviews。
- 最近职位使用卡片/列表显示。

### Jobs

- 搜索 Indeed 和 LinkedIn。
- 搜索词、地点、距离、发布时间和每次结果数可在 Settings 修改。
- 默认地点：Ottawa、Montreal、Toronto、Quebec City。
- 默认职位方向包括 Mechanical Engineer、Junior Mechanical Engineer、Mechanical Design Engineer、Mechanical Engineering Intern/Co-op、Aerospace Engineer/Intern。
- 搜索本身不需要 AI。
- 没有 AI 时仍会根据简历关键词、地点和 seniority 做 preliminary rule score。
- AI Analyze 后写入正式 match score、优势、缺失技能、风险、要求和模型名称。
- 支持多选（每批最多 20 条）进行 Analyze、Resume Drafts 和 Cover Drafts。
- 支持职位描述的可选中文翻译。
- 可手动 Add Job 或导入 CSV/JSON。
- 不自动投递；Open Job Page 只打开原职位网页。

### Resume & CV

- Settings 中手动选择原始简历。
- 支持 TXT、Markdown、PDF、DOCX。
- 原始文件复制保存，抽取文本用于分析。
- Resume Drafts 和 Cover Letters 在同一页面分标签显示。
- 简历支持 Original PDF、Generated PDF 和左右 Compare Changes。
- Compare 中旧内容为红色，新内容为绿色。
- 支持 Approve/Reject，不自动提交。

### Applications

- 仅作为人工申请状态追踪。
- 用户手动 Mark Selected as Applied。

### Settings

- 选择数据保存位置；迁移采用复制，保留原位置，避免破坏数据。
- 选择简历和额外资料。
- 填写技能、学历、经历、工作许可、语言、证照、可工作时间、期望职位和地点等资料。
- 配置本地 Ollama、OpenAI-compatible API、模型和 fallback。
- API key 加密后保存在用户数据目录，不应进入分发包。

## 5. 数据目录设计

本地开发版当前通过 `data-location.json` 指向 `D:\ApplyPilot`。

数据目录结构：

```text
D:\ApplyPilot\
  data\applypilot.db
  data\settings.json
  resumes\original\
  resumes\generated\
  resumes\approved\
  resumes\archive\
  supporting_documents\
  cover_letters\
  cache\
  logs\
  exports\
  backups\
```

冻结后的便携版默认使用 `%LOCALAPPDATA%\CareerOS\data`，不要把开发机的 `data-location.json`、数据库或个人文档打入测试包。

## 6. 本地源码版验证状态

最近一次自动测试：

```powershell
cd C:\Users\ksp23\Desktop\ApplyPilot
.\runtime\python.exe -B -m unittest discover -s tests -q
```

结果：`Ran 16 tests`，`OK`。

本地源码版使用 `START.cmd` 启动。之前已经处理过：

- 全局 Python 不完整时改用项目内 runtime。
- 日志文件被占用时启动失败。
- `QSize` 缺失导致启动闪退。
- PDF 中不兼容符号显示成问号。
- Resume/Cover Letter 页面、红绿对比、响应式 Settings、复选框和整体 UI。

## 7. 便携测试包当前故障（尚未修复）

用户明确要求暂时不继续修复，留给新任务处理。

第一版测试包在点击 Search 时报告：

```text
Failed to load dynlib/dll ...
_internal\tls_client\dependencies\tls-client-64.dll
```

随后使用 PyInstaller 的 `--collect-all tls_client` 重新构建 v2，并确认包内存在：

```text
CareerOS\_internal\tls_client\dependencies\tls-client-64.dll
```

v2 能完成普通启动测试，但用户实际点击 Search 时仍然直接闪退。也就是说，不能把“能打开主窗口”当成“搜索功能已验证”。当前桌面已找不到之前生成的 `CareerOS-Test-Windows-v2.zip`，不要依赖旧 ZIP；源码目录中的 `release-v2` 仍可能存在，但应重新诊断和构建。

下一任务建议先做：

1. 给冻结版增加不会随 GUI 退出而丢失的崩溃日志，例如 `%LOCALAPPDATA%\CareerOS\logs\crash.log`。
2. 捕获 `sys.excepthook`、`threading.excepthook`、Qt 后台线程异常和 Windows native crash 信息。
3. 建立一个无需点击 GUI 的 frozen search smoke test，至少实际执行一次 `jobspy.scrape_jobs`。
4. 用 console/debug PyInstaller build 复现 Search，而不是只做窗口启动测试。
5. 检查 `tls_client` 加载后的 VC++ runtime、证书、pandas/jobspy 动态导入、multiprocessing 和 Qt 线程边界。
6. 修复后再做全新用户目录、无 Python 环境和完整解压目录测试。

上次 PyInstaller 构建思路：

```powershell
.\runtime\Scripts\pyinstaller.exe --noconfirm --clean --windowed --onedir `
  --name CareerOS `
  --distpath .\release-v2 `
  --workpath .\build\pyinstaller-v2 `
  --specpath .\build `
  --add-data "C:\Users\ksp23\Desktop\ApplyPilot\assets;assets" `
  --collect-all tls_client `
  --hidden-import docx `
  --hidden-import pypdf `
  --hidden-import reportlab `
  main.py
```

不要直接重复发布这个命令的结果；必须先完成真实 Search smoke test。

## 8. 新任务建议验收标准

- 本地源码版双击 `START.cmd` 能打开。
- 无 AI/API key 时可以搜索职位。
- 测试包在一台没有 Python 的 Windows 10/11 x64 电脑上可打开。
- 测试包实际点击 Search 后不中止，能够保存至少一条结果或正常显示可读的网络错误。
- 搜索失败只能显示错误并写入日志，不能导致整个程序退出。
- 测试包不包含开发者的简历、数据库、设置、API key 或个人资料。
- 不存在自动投递功能。
- 16 项现有测试继续通过，并增加冻结版 Search 回归测试。

## 9. 可复制给新任务的开场请求

```text
请阅读 CareerOS-新任务交接说明-2026-08-27.md，然后继续处理 CareerOS。
实际源码在 C:\Users\ksp23\Desktop\ApplyPilot。
优先修复 PyInstaller 便携版点击 Search 直接闪退的问题；先加入持久崩溃日志和可重复的 frozen search smoke test，再定位依赖或线程原因。不要自动投递，不要删除或打包我的简历、数据库、个人资料和 API key。修复后必须在无 Python 的隔离环境验证实际 Search，不仅是启动窗口。
```

